class Solution {
public:
    bool judgeSquareSum(int c) {
       long a = 0; long b = sqrt(c);
        while(a<=b){
       long   res =( a*a + b*b);
            if(res==c)return true;
            else if (res<c) a++;
            else b--;
        }
        return false;
    }
};
